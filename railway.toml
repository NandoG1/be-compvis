[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 1 --timeout 300 --max-requests 50 --preload --worker-tmp-dir /dev/shm"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
